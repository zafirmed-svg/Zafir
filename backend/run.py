# run.py
import uvicorn

if __name__ == "__main__":
    # This project was migrated to Django; use `manage.py runserver` or
    # `uvicorn backend.asgi:application --host 0.0.0.0 --port 8000` to run the app.
